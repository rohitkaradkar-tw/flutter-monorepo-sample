name: monorepo_sample

packages:
  - packages/*
  - apps/*

scripts:
  analyze:
    exec: flutter analyze .
  pub_get:
    exec: flutter pub get
  clean:
    exec: flutter clean
  test_coverage:
    exec: flutter test --coverage
